---
description:py程序的一些代码规则
globs:
  - "**/*.py"
alwaysApply: false
---
1. 所有入口文件命名为run.py，项目需有虚拟环境，并提供start.py启动文件。start.py需自动进入虚拟环境，检查并安装依赖后启动run.py。

2. Python文件中，函数之间需有明显分界，避免代码混乱，提升可读性。
3. 若你在尝试调用程序时，不要进入python交互模式，这不方便后续代码执行，你只要通过命令行运行 Python 脚本